/** @type {import('tailwindcss').Config} */
module.exports = {
  // Tailwind v4: content is auto-detected, no need for `content` array.
  // Custom theme tokens are defined in app/globals.css via @theme.
  // Preflight is disabled — base styles are provided by globals.css.
};
