# Modern baseline targets. These browsers natively support the JS features
# Lighthouse flagged as needlessly polyfilled (Array.prototype.at/flat/flatMap,
# Object.fromEntries/hasOwn, String.prototype.trimStart/trimEnd), so SWC stops
# emitting those legacy polyfills and transforms. Binding constraints are
# Array.prototype.at / Object.hasOwn -> Safari >= 15.4, Chrome >= 93, FF >= 92.
Chrome >= 100
Edge >= 100
Firefox >= 100
Safari >= 15.4
iOS >= 15.4
not dead
